// const Export = require('./Export')
// console.log(Export.x)

// const arr =[10,20,32,322,34,3234,554,55];
// let result = arr.filter((items)=>{
//   return items > 30;
// })
// console.log(result)

// filter is used to filter the array elements based on the condition provided in the filter

// Global Modules :- these are the modules which we need not to define
// Non-Global Modules :- These are the modules which we need to define(i.e we need to call them/import them before using them)